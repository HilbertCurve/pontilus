#include "physics2d/CollisionResolution.h"
#include "physics2d/CollisionDetection.h"

namespace Pontilus
{
    namespace Physics2D
    {
        void freeResolve(pData data)
        {
            // determine collision normal based on body velocities

            // determine angular-velocity-based normal

            // determine tangential force based on vector between center
            // and collision point of each body

            // scale angular-velocity-based normal and tangential normal based
            // on conservation of momentum

            // resolve velocity changes

            // resolve angular velocity changes

        }
    }
}